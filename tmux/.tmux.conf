#--------------------------------------------------------------------------
# Configuration
#--------------------------------------------------------------------------

# Main project for quick session switching
set-environment -g WORK_MAIN_PROJECT "$WORK_MAIN_PROJECT"

# Use Vi mode
setw -g mode-keys vi

# Increase scrollback buffer size
set -g history-limit 25000

# Start window and pane numbering from 1 for easier switching
# set -g base-index 1
# setw -g pane-base-index 1

# Renumber windows when one is removed.
set -g renumber-windows on

# Improve colors

# Enable undercurl
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'

# Enable undercurl colors
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'

# Allow the mouse to resize windows and select tabs
set -g mouse on

# Allow tmux to set the terminal title
set -g set-titles on

# Monitor window activity to display in the status bar
setw -g monitor-activity on

# A bell in another window should cause a bell in the current window
set -g bell-action any

# Don't show distracting notifications
set -g visual-bell off
set -g visual-activity off

# Focus events enabled for terminals that support them
set -g focus-events on

# Useful when using sharing a session with different size terminals
# setw -g aggressive-resize on

# don't detach tmux when killing a session
# set -g detach-on-destroy off

# address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0

# Update status bar every N seconds
set -g status-interval 1

set -as terminal-features ",*:RGB"
set -g default-terminal "tmux-256color"
# set -ag terminal-overrides ",xterm-256color:RGB"

# Dim inactive panes
# set -g window-style 'fg=colour247,bg=colour236'
# set -g window-active-style 'fg=colour250,bg=black'

# Status bar position
set -g status-position top

# skip "kill-pane 1? (y/n)" prompt
bind-key x kill-pane

# don't exit from tmux when closing a session
set -g detach-on-destroy off

# Open Windows & Panes in the current directory
bind  c  new-window      -c "#{pane_current_path}"
bind  %  split-window -h -c "#{pane_current_path}"
bind '"' split-window -v -c "#{pane_current_path}"

# PREFIX r to reload of the config file
unbind r
bind r source-file ~/.tmux.conf;

# Open Lazygit in the current directory
bind g new-window -S -c "#{pane_current_path}" -n "" "tmux setw window-status-activity-style none \; lazygit"

# Open Github Dashboard in the current directory
bind G new-window -S -c "#{pane_current_path}" -n "" "tmux setw window-status-activity-style none \; ghdash"

# Open Claude Code in the current directory
bind C new-window -S -c "#{pane_current_path}" -n "" "tmux setw window-status-activity-style none \; fnm use default; claude"

# Allow holding Ctrl when using using prefix+p/n/l for switching windows
bind C-p previous-window
bind C-n next-window

bind-key -n M-1 run-shell "sesh connect #{WORK_MAIN_PROJECT}"
bind-key -n M-2 run-shell "sesh connect Brain"
bind-key -n M-3 run-shell "sesh connect dotf"

# Switch to the previous session
bind-key -n M-l switch-client -l

# Allow holding Ctrl to maximize the pane
bind C-m resize-pane -Z
bind m resize-pane -Z

# Allow holding Ctrl to clear the pane
bind C-f send-keys -R \; clear-history \; send-keys C-l 
bind f send-keys -R \; clear-history \; send-keys C-l

# open an application in a new window with prefix + S-o, and allow me to provide the app name
bind-key O command-prompt -p "open app: " "new-window '%%'"

# Tmux Monorepo Manager
bind-key k run-shell 'sesh connect "$(sesh list -t | fzf-tmux -p 40%,60% \
  --no-sort --border-label " sesh " --prompt "⚡  " \
  --header "  C-a all | C-t tmux | C-g configs | C-x zoxide | C-f find" \
  --bind "tab:down,btab:up" \
  --bind "ctrl-a:change-prompt(⚡  )+reload(sesh list)" \
  --bind "ctrl-t:change-prompt(🪟  )+reload(sesh list -t)" \
  --bind "ctrl-g:change-prompt(⚙️  )+reload(sesh list -c)" \
  --bind "ctrl-x:change-prompt(📁  )+reload(sesh list -z)" \
  --bind "ctrl-r:change-prompt(🔎  )+reload(fd -H -d 1 -t d -E .Trash . ~/com.github/)" \
)"'

#--------------------------------------------------------------------------
# Plugins
#--------------------------------------------------------------------------

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'schasse/tmux-jump'
set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'catppuccin/tmux'

# For tmux-open, Shift+s to search selection in DDG
set -g @open-S 'https://www.duckduckgo.com/?q='

# --------------------------------------------------------------------------
# Theme
# --------------------------------------------------------------------------

set -g status-left "[ #S ] "
set -g status-left-length 100
set -g status-right-length 100
# --------

set -g @catppuccin_menu_selected_style "fg=#{@thm_fg},bold,bg=#{@thm_overlay_0}"

# Pane styling options
set -g @catppuccin_pane_status_enabled "no" # yes, no, d:yes
set -g @catppuccin_pane_border_status "off" # yes, off, d:yes
set -g @catppuccin_pane_border_style "fg=#{@thm_overlay_0}" # d:fg=#{@thm_overlay_0}
set -g @catppuccin_pane_active_border_style "##{?pane_in_mode,fg=#{@thm_lavender},##{?pane_synchronized,fg=#{@thm_mauve},fg=#{@thm_lavender}}}"
set -g @catppuccin_pane_left_separator "█" # d:█
set -g @catppuccin_pane_middle_separator "█" # d:█
set -g @catppuccin_pane_right_separator "█" # d:█
set -g @catppuccin_pane_color "#{@thm_green}" # d:#{@thm_green}
set -g @catppuccin_pane_background_color "none" # d:#{@thm_surface_0}
set -g @catppuccin_pane_default_text "#W" # d:##{b:pane_current_path}
set -g @catppuccin_pane_default_fill "number" # d:number
set -g @catppuccin_pane_number_position "left" # right, left, d:left

set -g @catppuccin_window_status_style "basic" # basic, rounded, slanted, custom, none, d:basic
set -g @catppuccin_window_text_color "#{@thm_surface_0}" # d:#{@thm_surface_0}
set -g @catppuccin_window_number_color "#{@thm_overlay_2}" # d:#{@thm_overlay_2
set -g @catppuccin_window_text " #W" # d:#T
set -g @catppuccin_window_number "#I" # d:#I
set -g @catppuccin_window_current_text_color "#{@thm_crust}" # d:#{@thm_surface_1}
set -g @catppuccin_window_current_number_color "#{@thm_green}" # d:#{@thm_mauve}
set -g @catppuccin_window_current_text " #W" # d: #T
set -g @catppuccin_window_current_number "#I" # d:#I
set -g @catppuccin_window_number_position "left" # d:left

set -g @catppuccin_window_flags "icon" # none, icon, text, d:none
set -g @catppuccin_window_flags_icon_last "" # d: 󰖰
set -g @catppuccin_window_flags_icon_current "" # d: 󰖯
set -g @catppuccin_window_flags_icon_zoom " 󰁌" # d: 󰁌
set -g @catppuccin_window_flags_icon_mark "" # d: 󰃀
set -g @catppuccin_window_flags_icon_silent "" # d: 󰂛
set -g @catppuccin_window_flags_icon_activity "" # d: 󱅫
set -g @catppuccin_window_flags_icon_bell "" # d: 󰂞
# Matches icon order when using `#F` (`#!~[*-]MZ`)
set -g @catppuccin_window_flags_icon_format "##{?window_activity_flag,#{E:@catppuccin_window_flags_icon_activity},}##{?window_bell_flag,#{E:@catppuccin_window_flags_icon_bell},}##{?window_silence_flag,#{E:@catppuccin_window_flags_icon_silent},}##{?window_active,#{E:@catppuccin_window_flags_icon_current},}##{?window_last_flag,#{E:@catppuccin_window_flags_icon_last},}##{?window_marked_flag,#{E:@catppuccin_window_flags_icon_mark},}##{?window_zoomed_flag,#{E:@catppuccin_window_flags_icon_zoom},} "

# Status line options
set -g @catppuccin_status_left_separator "" # d:
set -g @catppuccin_status_middle_separator " " # d:
set -g @catppuccin_status_right_separator "█" # d:█
set -g @catppuccin_status_connect_separator "no" # yes, no, d:yes
set -g @catppuccin_status_fill "icon" # d:icon
set -g @catppuccin_status_module_bg_color "#{@thm_surface_0}" # d:#{@thm_surface_0}

# Overrides
set -g @catppuccin_status_background "none"

# Fix window separators for none background
set -g @catppuccin_window_left_separator " "
set -g @catppuccin_window_right_separator " "
set -g @catppuccin_window_current_left_separator " "
set -g @catppuccin_window_current_right_separator " "

# Disable continuum status display
set -g @continuum-save-interval '0'
set -g @catppuccin_date_time_text " %b %d, %H:%M"

# Module overrides (must be before status modules)
set -g @catppuccin_gitmux_text " #(gitmux_text \"#{pane_current_path}\")"
set -g @catppuccin_battery_icon "#(~/.tmux/plugins/tmux-battery/scripts/battery_icon.sh)"
set -g @catppuccin_battery_text " #(~/.tmux/plugins/tmux-battery/scripts/battery_percentage.sh)"

# Status modules
set -g @catppuccin_status_modules_right "gitmux open_pr_count keyboard_layout battery date_time"

# --------------------------------------------------------------------------

run '~/.tmux/plugins/tpm/tpm'

# Load custom modules after theme variables are set
source "~/.config/tmux/modules/keyboard_layout.conf"
source "~/.config/tmux/modules/open_pr_count.conf"

# Override gitmux text after TPM loads to use custom script
set -g @catppuccin_gitmux_text " #(gitmux_text \"#{pane_current_path}\")"

# Override status-right after plugins load to remove any plugin additions
set -g status-right "#{E:@catppuccin_status_gitmux}#{E:@catppuccin_status_open_pr_count}#{E:@catppuccin_status_keyboard_layout}#{E:@catppuccin_status_battery}#{E:@catppuccin_status_date_time}"

# Add bold styling to current window
set -g window-status-current-style "bold"

# Apply automatic renaming settings after plugins load to prevent overrides
set -g allow-rename on
set -g automatic-rename on
set -g automatic-rename-format '#{?#{==:#{pane_current_command},zsh},#{b:pane_current_path},#{pane_current_command}}'

# Restore last-window bindings after plugins (vim-tmux-navigator overrides C-l)
bind C-l last-window
bind l last-window

# Force refresh status line and windows
# run-shell 'tmux refresh-client -S'

# Hook to refresh status line after session creation
set-hook -g after-new-session 'run-shell "sleep 0.2; tmux source-file ~/.tmux.conf"'

